{% extends 'formation/base.html.twig' %}

{% block title %}New Formation{% endblock %}

{% block body %}
    <h1>Create new Formation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">{{ button_label|default('Save') }}</button>
        <a href="{{ path('app_formation_index_back') }}" class="btn btn-secondary">Back to list</a>
    {{ form_end(form) }}
{% endblock %}