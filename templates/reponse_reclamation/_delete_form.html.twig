<form method="post" action="{{ path('app_reponse_reclamation_delete', {'id': reponse_reclamation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette réponse ?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse_reclamation.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
