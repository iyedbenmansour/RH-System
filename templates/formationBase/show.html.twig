{% extends 'formation/base.html.twig' %}

{% block title %}Formation details{% endblock %}

{% block body %}
    <h1>Formation details</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ formation.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ formation.description }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ formation.duree }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ formation.prix }} €</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ formation.type }}</td>
            </tr>
            <tr>
                <th>Trainer</th>
                <td>{{ formation.formateur }}</td>
            </tr>
            <tr>
                <th>Participants</th>
                <td>{{ formation.nbparticipant }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ formation.status }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex gap-2">
        <a href="{{ path('app_formation_index') }}" class="btn btn-secondary">Back to list</a>
        <a href="{{ path('app_formation_edit', {'id': formation.id}) }}" class="btn btn-warning">Edit</a>
        {{ include('formation/_delete_form.html.twig') }}
    </div>
{% endblock %}