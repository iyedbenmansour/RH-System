<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formation {{ formation.titre }}</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(to right, #6a4cff, #231a6f);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 100vh;
            flex-direction: column;
            padding: 20px;
        }

        .page-title {
            width: 100%;
            text-align: center;
            font-size: 2.7rem;
            color: rgb(255, 255, 255);
            margin: 30px 0px;
            font-weight: 600;
        }

        .form-container {
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 800px;
            padding: 40px;
            margin: 0 auto;
        }

        .form-container:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
        }

        .card {
            background-color: #f9f9f9;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .card h3 {
            font-size: 1.5rem;
            color: #231a6f;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .card p {
            font-size: 1rem;
            color: #333;
            margin: 5px 0;
        }

        .card i {
            margin-right: 8px;
            color: #6a4cff;
        }

        .form-actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
        }

        .form-actions .btn {
            padding: 12px 20px;
            border-radius: 8px;
            color: white;
            font-size: 16px;
            text-decoration: none;
            text-align: center;
            width: 200px;
            transition: background-color 0.3s, transform 0.3s;
        }

        .form-actions .btn:hover {
            transform: scale(1.05);
        }

        .back-btn {
            background: linear-gradient(45deg, #231a6f, #4b3c9e);
        }

        .edit-btn {
            color: black !important;
            background: linear-gradient(45deg, rgb(192, 191, 191), rgb(143, 142, 142));
        }

        .edit-btn:hover {
            background-color: #e67e22;
        }

        .delete-btn {
            background: linear-gradient(45deg, #e74c3c, #c0392b) !important;
        }

        .delete-btn:hover {
            background-color: #c0392b;
        }

        @media (max-width: 768px) {
            .page-title {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>

    <h1 class="page-title">Formation Details</h1>

    <div class="form-container">
        <div class="card">
            <h3><i class="fas fa-info-circle"></i> Formation Information</h3>
            <!--<p><i class="fas fa-id-card"></i> ID: {{ formation.id }}</p>-->
            <p><i class="fas fa-heading"></i> Title: {{ formation.titre }}</p>
            <p><i class="fas fa-file-alt"></i> Description: {{ formation.description }}</p>
            <p><i class="fas fa-clock"></i> Duration: {{ formation.duree }}</p>
            <p><i class="fas fa-money-bill-wave"></i> Price: {{ formation.prix }} DT</p>
            <p><i class="fas fa-tags"></i> Type: {{ formation.type }}</p>
        </div>

        <div class="card">
            <h3><i class="fas fa-users"></i> Trainer & Participants</h3>
            <p><i class="fas fa-user-tie"></i> Trainer: {{ formation.formateur }}</p>
            <p><i class="fas fa-user-friends"></i> Participants: {{ formation.nbparticipant }}</p>
        </div>

        <div class="card">
            <h3><i class="fas fa-tasks"></i> Status</h3>
            <p><i class="fas fa-check-circle"></i> Status: {{ formation.status }}</p>
        </div>

        <div class="form-actions">
            <a href="{{ path('app_event_index') }}" class="btn back-btn"><i class="fas fa-arrow-left"></i> Back to List</a>
        </div>
    </div>

</body>
</html>