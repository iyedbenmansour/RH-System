<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Employee ID Card</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .id-card {
            width: 320px;
            height: 500px;
            background-color: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        .company-header {
            background-color: #3498db;
            height: 80px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            font-size: 24px;
            border-radius: 15px 15px 0 0;
        }
        .photo-container {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background-color: #f0f3f6;
            margin: 20px auto;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        .employee-name {
            font-size: 22px;
            font-weight: bold;
            color: #2c3e50;
            text-align: center;
            margin: 10px 0;
        }
        .employee-position {
            font-size: 18px;
            color: #7f8c8d;
            text-align: center;
            margin-bottom: 20px;
        }
        .info-box {
            background-color: #f5f7fa;
            padding: 15px;
            border-radius: 8px;
            width: 270px;
            margin: 0 auto 20px;
        }
        .info-item {
            font-size: 16px;
            color: #34495e;
            margin: 5px 0;
        }
        .barcode-area {
            width: 200px;
            height: 60px;
            margin: 20px auto;
            text-align: center;
        }
        .signature-text {
            font-size: 12px;
            color: #95a5a6;
            font-style: italic;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="id-card">
        <div class="company-header">COMPANY NAME</div>
        <div style="padding: 25px;">
            <div class="photo-container">
                {% if formData.photoPath %}
                    <img src="{{ app.request.schemeAndHttpHost ~ formData.photoPath }}" style="max-width: 100%; max-height: 100%;">
                {% else %}
                    <span style="color: #95a5a6;">No photo</span>
                {% endif %}
            </div>
            <div class="employee-name">{{ formData.fullName ?: 'Full Name' }}</div>
            <div class="employee-position">{{ formData.position ?: 'Position' }}</div>
            <div class="info-box">
                <div class="info-item">ID: {{ formData.id ?: '0000' }}</div>
                <div class="info-item">Tel: {{ formData.telephone ?: '000-000-0000' }}</div>
                <div class="info-item">
                    Valid: {{ formData.startDate ? formData.startDate|date('d M Y') : 'Start' }} - 
                    {{ formData.endDate ? formData.endDate|date('d M Y') : 'End' }}
                </div>
            </div>
            <div class="barcode-area">
                {# If barcode_image starts with data:image, show as img; otherwise output as HTML (ASCII) #}
                {% if barcode_image starts with 'data:image' %}
                    <img src="{{ barcode_image }}" alt="Employee Barcode" style="width: 100%; height: 100%;">
                {% else %}
                    {{ barcode_image|raw }}
                {% endif %}
            </div>
            <div class="signature-text">Authorized Signature</div>
        </div>
    </div>
</body>
</html>