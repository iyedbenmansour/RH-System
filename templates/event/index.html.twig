<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event List</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(to right, #6a4cff, #231a6f);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 100vh;
            flex-direction: column;
            padding: 20px;
        }

        .page-title {
            width:100%;
            text-align: center;
            font-size: 2.7rem;
            color:rgb(255, 255, 255);
            margin-bottom: 30px;
            font-weight: 500;
        }

        /* Cards Layout */
        .cards-container {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
            padding: 20px;
            width: 100%;
            max-width: 1300px;
            margin-top: 20px;
        }

        .card {
            background-color: #fff;
            border-radius: 6px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }

        .card h3 {
            font-size: 1.8rem;
            color: #231a6f;
            font-weight: 500;
            margin-bottom: 15px;
        }

        .card p {
            font-size: 1.1rem;
            color: #555;
            margin-bottom: 15px;
        }

        .card .btn-container {
            display: flex;
            justify-content: space-around;
            margin-top: 15px;
        }

        .card .btn {
            padding: 15px 18px;
            border-radius: 15px;
            color: white;
            text-decoration: none;
            font-size: 16px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .card .btn i {
            margin-right: 8px;
        }

        .show-btn {
            background: linear-gradient(45deg, #231a6f, #4b3c9e);
        }

        .show-btn:hover {
            background-color: #1d1460;
            transform: scale(1.05);
        }

        .edit-btn {
            background: linear-gradient(45deg,rgb(192, 191, 191),rgb(143, 142, 142));
        }

        .edit-btn:hover {
            background-color: #e67e22;
            transform: scale(1.05);
        }

        .delete-btn {
            cursor:pointer;
            background: linear-gradient(45deg, #e74c3c, #c0392b);
        }

        .delete-btn:hover {
            background-color: #c0392b;
            transform: scale(1.05);
        }
        .display-btn{
            background: linear-gradient(45deg,rgb(38, 148, 30),rgb(52, 182, 20));
        }
        .display-btn:hover {
            background-color:rgba(38, 148, 30, 0.57);
            transform: scale(1.05);
        }

        .create-btn {
            background: linear-gradient(45deg,rgb(7, 7, 7),rgb(23, 25, 24));
            color: #fff;
            padding: 12px 20px;
            border-radius: 8px;
            text-decoration: none;
            font-size: 16px;
            display: block;
            width: 200px;
            margin: 20px auto;
            text-align: center;
            transition: background-color 0.3s, transform 0.3s;
        }

        .create-btn:hover {
            background-color:rgb(7, 7, 7);
            transform: scale(1.05);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .page-title {
                font-size: 2rem;
            }

            .cards-container {
                grid-template-columns: repeat(2, 1fr);
            }

            .card {
                padding: 15px;
            }

            .btn {
                font-size: 12px;
                padding: 8px 12px;
            }

            .create-btn {
                width: 180px;
            }

            .cards-container {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>

<h1 class="page-title">Events List</h1>

<div class="cards-container">
    {% for event in events %}
        <div class="card">
            <h3>{{ event.name }}</h3>
            <p><strong>Date:</strong> {{ event.date ? event.date|date('Y-m-d') : '' }}</p>
            <p><strong>Location:</strong> {{ event.location }}</p>
            <p><strong>Organiser:</strong> {{ event.organiser }}</p>
            <p><strong>Type:</strong> {{ event.eventType }}</p>
            <p><strong>Participants:</strong> {{ event.nbParticipant }}</p>
            <p><strong>Price:</strong> {{ event.ticketPrice }} DT</p>
            
            <div class="btn-container">
                <a href="{{ path('app_event_show', {'id': event.id}) }}" class="btn show-btn"><i class="fas fa-eye"></i> Show</a>
                <!--<a href="{{ path('app_event_edit', {'id': event.id}) }}" class="btn edit-btn"><i class="fas fa-edit"></i> Edit</a>
                <form method="post" action="{{ path('app_event_delete', {'id': event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="inline-form">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
                    <button class="btn delete-btn"><i class="fas fa-trash-alt"></i> Delete</button>
                </form>-->
                <a href="{% if event.hasFormation %}{{ path('app_formation_show', {'id': event.formationId}) }}{% else %}#{% endif %}" class="btn display-btn" {% if not event.hasFormation %}style="pointer-events: none; opacity: 0.5;"{% endif %}>
                    <i class="fas fa-chalkboard-teacher"></i> Display Formation
                </a>
            </div>
        </div>
    {% else %}
        <div class="no-events">No events found</div>
    {% endfor %}
</div>

<!--<a href="{{ path('app_event_new') }}" class="btn create-btn"><i class="fas fa-plus-circle"></i> Create New Event</a>-->

</body>
</html>
